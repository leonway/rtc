import React, { useEffect } from 'react'

interface NotFoundProps {}

const NotFound: React.FC<NotFoundProps> = (componentProps) => {
  useEffect(() => {}, [])
  return <div>404</div>
}

export default NotFound
